begin_unit
begin_comment
comment|/**************************************************************************** ** ** Copyright (C) 2012 Digia Plc and/or its subsidiary(-ies). ** Contact: http://www.qt-project.org/legal ** ** This file is part of the QtGui module of the Qt Toolkit. ** ** $QT_BEGIN_LICENSE:LGPL$ ** Commercial License Usage ** Licensees holding valid commercial Qt licenses may use this file in ** accordance with the commercial license agreement provided with the ** Software or, alternatively, in accordance with the terms contained in ** a written agreement between you and Digia.  For licensing terms and ** conditions see http://qt.digia.com/licensing.  For further information ** use the contact form at http://qt.digia.com/contact-us. ** ** GNU Lesser General Public License Usage ** Alternatively, this file may be used under the terms of the GNU Lesser ** General Public License version 2.1 as published by the Free Software ** Foundation and appearing in the file LICENSE.LGPL included in the ** packaging of this file.  Please review the following information to ** ensure the GNU Lesser General Public License version 2.1 requirements ** will be met: http://www.gnu.org/licenses/old-licenses/lgpl-2.1.html. ** ** In addition, as a special exception, Digia gives you certain additional ** rights.  These rights are described in the Digia Qt LGPL Exception ** version 1.1, included in the file LGPL_EXCEPTION.txt in this package. ** ** GNU General Public License Usage ** Alternatively, this file may be used under the terms of the GNU ** General Public License version 3.0 as published by the Free Software ** Foundation and appearing in the file LICENSE.GPL included in the ** packaging of this file.  Please review the following information to ** ensure the GNU General Public License version 3.0 requirements will be ** met: http://www.gnu.org/copyleft/gpl.html. ** ** ** $QT_END_LICENSE$ ** ****************************************************************************/
end_comment
begin_include
include|#
directive|include
file|"qglobal.h"
end_include
begin_if
if|#
directive|if
operator|!
name|defined
argument_list|(
name|QT_NO_RAWFONT
argument_list|)
end_if
begin_include
include|#
directive|include
file|"qrawfont.h"
end_include
begin_include
include|#
directive|include
file|"qrawfont_p.h"
end_include
begin_include
include|#
directive|include
file|<QtCore/qendian.h>
end_include
begin_macro
name|QT_BEGIN_NAMESPACE
end_macro
begin_comment
comment|/*!    \class QRawFont    \brief The QRawFont class provides access to a single physical instance of a font.    \since 4.8     \inmodule QtGui     \ingroup text    \mainclass     \note QRawFont is a low level class. For most purposes QFont is a more appropriate class.     Most commonly, when presenting text in a user interface, the exact fonts used    to render the characters is to some extent unknown. This can be the case for several    reasons: For instance, the actual, physical fonts present on the target system could be    unexpected to the developers, or the text could contain user selected styles, sizes or    writing systems that are not supported by font chosen in the code.     Therefore, Qt's QFont class really represents a query for fonts. When text is interpreted,    Qt will do its best to match the text to the query, but depending on the support, different    fonts can be used behind the scenes.     For most use cases, this is both expected and necessary, as it minimizes the possibility of    text in the user interface being undisplayable. In some cases, however, more direct control    over the process might be useful. It is for these use cases the QRawFont class exists.     A QRawFont object represents a single, physical instance of a given font in a given pixel size.    I.e. in the typical case it represents a set of TrueType or OpenType font tables and uses a    user specified pixel size to convert metrics into logical pixel units. It can be used in    combination with the QGlyphRun class to draw specific glyph indexes at specific positions, and    also have accessors to some relevant data in the physical font.     QRawFont only provides support for the main font technologies: GDI and DirectWrite on Windows    platforms, FreeType on Linux platforms and CoreText on Mac OS X. For other    font back-ends, the APIs will be disabled.     QRawFont can be constructed in a number of ways:    \list    \li It can be constructed by calling QTextLayout::glyphs() or QTextFragment::glyphs(). The       returned QGlyphs objects will contain QRawFont objects which represent the actual fonts       used to render each portion of the text.    \li It can be constructed by passing a QFont object to QRawFont::fromFont(). The function       will return a QRawFont object representing the font that will be selected as response to       the QFont query and the selected writing system.    \li It can be constructed by passing a file name or QByteArray directly to the QRawFont       constructor, or by calling loadFromFile() or loadFromData(). In this case, the       font will not be registered in QFontDatabase, and it will not be available as part of       regular font selection.    \endlist     QRawFont is considered local to the thread in which it is constructed (either using a    constructor, or by calling loadFromData() or loadFromFile()). The QRawFont cannot be moved to a    different thread, but will have to be recreated in the thread in question.     \note For the requirement of caching glyph indexes and font selections for static text to avoid    reshaping and relayouting in the inner loop of an application, a better choice is the QStaticText    class, since it optimizes the memory cost of the cache and also provides the possibility of paint    engine specific caches for an additional speed-up. */
end_comment
begin_comment
comment|/*!     \enum QRawFont::AntialiasingType      This enum represents the different ways a glyph can be rasterized in the function     alphaMapForGlyph().      \value PixelAntialiasing Will rasterize by measuring the coverage of the shape on whole pixels.            The returned image contains the alpha values of each pixel based on the coverage of            the glyph shape.     \value SubPixelAntialiasing Will rasterize by measuring the coverage of each subpixel,            returning a separate alpha value for each of the red, green and blue components of            each pixel. */
end_comment
begin_comment
comment|/*!    Constructs an invalid QRawFont. */
end_comment
begin_constructor
DECL|function|QRawFont
name|QRawFont
operator|::
name|QRawFont
parameter_list|()
member_init_list|:
name|d
argument_list|(
operator|new
name|QRawFontPrivate
argument_list|)
block|{ }
end_constructor
begin_comment
comment|/*!    Constructs a QRawFont representing the font contained in the file referenced    by \a fileName for the size (in pixels) given by \a pixelSize, and using the    hinting preference specified by \a hintingPreference.     \note The referenced file must contain a TrueType or OpenType font. */
end_comment
begin_constructor
DECL|function|QRawFont
name|QRawFont
operator|::
name|QRawFont
parameter_list|(
specifier|const
name|QString
modifier|&
name|fileName
parameter_list|,
name|qreal
name|pixelSize
parameter_list|,
name|QFont
operator|::
name|HintingPreference
name|hintingPreference
parameter_list|)
member_init_list|:
name|d
argument_list|(
operator|new
name|QRawFontPrivate
argument_list|)
block|{
name|loadFromFile
argument_list|(
name|fileName
argument_list|,
name|pixelSize
argument_list|,
name|hintingPreference
argument_list|)
expr_stmt|;
block|}
end_constructor
begin_comment
comment|/*!    Constructs a QRawFont representing the font contained in the supplied    \a fontData for the size (in pixels) given by \a pixelSize, and using the    hinting preference specified by \a hintingPreference.     \note The data must contain a TrueType or OpenType font. */
end_comment
begin_constructor
DECL|function|QRawFont
name|QRawFont
operator|::
name|QRawFont
parameter_list|(
specifier|const
name|QByteArray
modifier|&
name|fontData
parameter_list|,
name|qreal
name|pixelSize
parameter_list|,
name|QFont
operator|::
name|HintingPreference
name|hintingPreference
parameter_list|)
member_init_list|:
name|d
argument_list|(
operator|new
name|QRawFontPrivate
argument_list|)
block|{
name|loadFromData
argument_list|(
name|fontData
argument_list|,
name|pixelSize
argument_list|,
name|hintingPreference
argument_list|)
expr_stmt|;
block|}
end_constructor
begin_comment
comment|/*!    Creates a QRawFont which is a copy of \a other. */
end_comment
begin_constructor
DECL|function|QRawFont
name|QRawFont
operator|::
name|QRawFont
parameter_list|(
specifier|const
name|QRawFont
modifier|&
name|other
parameter_list|)
block|{
name|d
operator|=
name|other
operator|.
name|d
expr_stmt|;
block|}
end_constructor
begin_comment
comment|/*!    Destroys the QRawFont */
end_comment
begin_destructor
DECL|function|~QRawFont
name|QRawFont
operator|::
name|~
name|QRawFont
parameter_list|()
block|{ }
end_destructor
begin_comment
comment|/*!   Assigns \a other to this QRawFont. */
end_comment
begin_function
DECL|function|operator =
name|QRawFont
modifier|&
name|QRawFont
operator|::
name|operator
name|=
parameter_list|(
specifier|const
name|QRawFont
modifier|&
name|other
parameter_list|)
block|{
name|d
operator|=
name|other
operator|.
name|d
expr_stmt|;
return|return
operator|*
name|this
return|;
block|}
end_function
begin_comment
comment|/*!   \fn void QRawFont::swap(QRawFont&other)   \since 5.0    Swaps this raw font with \a other. This function is very fast and   never fails. */
end_comment
begin_comment
comment|/*!    Returns true if the QRawFont is valid and false otherwise. */
end_comment
begin_function
DECL|function|isValid
name|bool
name|QRawFont
operator|::
name|isValid
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
return|;
block|}
end_function
begin_comment
comment|/*!    Replaces the current QRawFont with the contents of the file referenced    by \a fileName for the size (in pixels) given by \a pixelSize, and using the    hinting preference specified by \a hintingPreference.     The file must reference a TrueType or OpenType font.     \sa loadFromData() */
end_comment
begin_function
DECL|function|loadFromFile
name|void
name|QRawFont
operator|::
name|loadFromFile
parameter_list|(
specifier|const
name|QString
modifier|&
name|fileName
parameter_list|,
name|qreal
name|pixelSize
parameter_list|,
name|QFont
operator|::
name|HintingPreference
name|hintingPreference
parameter_list|)
block|{
name|QFile
name|file
argument_list|(
name|fileName
argument_list|)
decl_stmt|;
if|if
condition|(
name|file
operator|.
name|open
argument_list|(
name|QIODevice
operator|::
name|ReadOnly
argument_list|)
condition|)
name|loadFromData
argument_list|(
name|file
operator|.
name|readAll
argument_list|()
argument_list|,
name|pixelSize
argument_list|,
name|hintingPreference
argument_list|)
expr_stmt|;
block|}
end_function
begin_comment
comment|/*!    Replaces the current QRawFont with the font contained in the supplied    \a fontData for the size (in pixels) given by \a pixelSize, and using the    hinting preference specified by \a hintingPreference.     The \a fontData must contain a TrueType or OpenType font.     \sa loadFromFile() */
end_comment
begin_function
DECL|function|loadFromData
name|void
name|QRawFont
operator|::
name|loadFromData
parameter_list|(
specifier|const
name|QByteArray
modifier|&
name|fontData
parameter_list|,
name|qreal
name|pixelSize
parameter_list|,
name|QFont
operator|::
name|HintingPreference
name|hintingPreference
parameter_list|)
block|{
name|d
operator|.
name|detach
argument_list|()
expr_stmt|;
name|d
operator|->
name|cleanUp
argument_list|()
expr_stmt|;
name|d
operator|->
name|hintingPreference
operator|=
name|hintingPreference
expr_stmt|;
name|d
operator|->
name|thread
operator|=
name|QThread
operator|::
name|currentThread
argument_list|()
expr_stmt|;
name|d
operator|->
name|platformLoadFromData
argument_list|(
name|fontData
argument_list|,
name|pixelSize
argument_list|,
name|hintingPreference
argument_list|)
expr_stmt|;
block|}
end_function
begin_comment
comment|/*!    This function returns a rasterized image of the glyph at the given    \a glyphIndex in the underlying font, using the \a transform specified.    If the QRawFont is not valid, this function will return an invalid QImage.     If \a antialiasingType is set to QRawFont::SubPixelAntialiasing, then the resulting image will be    in QImage::Format_RGB32 and the RGB values of each pixel will represent the subpixel opacities of    the pixel in the rasterization of the glyph. Otherwise, the image will be in the format of    QImage::Format_Indexed8 and each pixel will contain the opacity of the pixel in the    rasterization.     \sa pathForGlyph(), QPainter::drawGlyphRun() */
end_comment
begin_function
DECL|function|alphaMapForGlyph
name|QImage
name|QRawFont
operator|::
name|alphaMapForGlyph
parameter_list|(
name|quint32
name|glyphIndex
parameter_list|,
name|AntialiasingType
name|antialiasingType
parameter_list|,
specifier|const
name|QTransform
modifier|&
name|transform
parameter_list|)
specifier|const
block|{
if|if
condition|(
operator|!
name|d
operator|->
name|isValid
argument_list|()
condition|)
return|return
name|QImage
argument_list|()
return|;
if|if
condition|(
name|antialiasingType
operator|==
name|SubPixelAntialiasing
condition|)
return|return
name|d
operator|->
name|fontEngine
operator|->
name|alphaRGBMapForGlyph
argument_list|(
name|glyphIndex
argument_list|,
name|QFixed
argument_list|()
argument_list|,
name|transform
argument_list|)
return|;
return|return
name|d
operator|->
name|fontEngine
operator|->
name|alphaMapForGlyph
argument_list|(
name|glyphIndex
argument_list|,
name|QFixed
argument_list|()
argument_list|,
name|transform
argument_list|)
return|;
block|}
end_function
begin_comment
comment|/*!    This function returns the shape of the glyph at a given \a glyphIndex in the underlying font    if the QRawFont is valid. Otherwise, it returns an empty QPainterPath.     The returned glyph will always be unhinted.     \sa alphaMapForGlyph(), QPainterPath::addText() */
end_comment
begin_function
DECL|function|pathForGlyph
name|QPainterPath
name|QRawFont
operator|::
name|pathForGlyph
parameter_list|(
name|quint32
name|glyphIndex
parameter_list|)
specifier|const
block|{
if|if
condition|(
operator|!
name|d
operator|->
name|isValid
argument_list|()
condition|)
return|return
name|QPainterPath
argument_list|()
return|;
name|QFixedPoint
name|position
decl_stmt|;
name|QPainterPath
name|path
decl_stmt|;
name|d
operator|->
name|fontEngine
operator|->
name|addGlyphsToPath
argument_list|(
operator|&
name|glyphIndex
argument_list|,
operator|&
name|position
argument_list|,
literal|1
argument_list|,
operator|&
name|path
argument_list|,
literal|0
argument_list|)
expr_stmt|;
return|return
name|path
return|;
block|}
end_function
begin_comment
comment|/*!    Returns true if this QRawFont is equal to \a other. Otherwise, returns false. */
end_comment
begin_function
DECL|function|operator ==
name|bool
name|QRawFont
operator|::
name|operator
name|==
parameter_list|(
specifier|const
name|QRawFont
modifier|&
name|other
parameter_list|)
specifier|const
block|{
return|return
name|d
operator|->
name|fontEngine
operator|==
name|other
operator|.
name|d
operator|->
name|fontEngine
return|;
block|}
end_function
begin_comment
comment|/*!     \fn bool QRawFont::operator!=(const QRawFont&other) const      Returns true if this QRawFont is not equal to \a other. Otherwise, returns false. */
end_comment
begin_comment
comment|/*!    Returns the ascent of this QRawFont in pixel units.     \sa QFontMetricsF::ascent() */
end_comment
begin_function
DECL|function|ascent
name|qreal
name|QRawFont
operator|::
name|ascent
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|ascent
argument_list|()
operator|.
name|toReal
argument_list|()
else|:
literal|0.0
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the descent of this QRawFont in pixel units.     \sa QFontMetricsF::descent() */
end_comment
begin_function
DECL|function|descent
name|qreal
name|QRawFont
operator|::
name|descent
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|descent
argument_list|()
operator|.
name|toReal
argument_list|()
else|:
literal|0.0
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the xHeight of this QRawFont in pixel units.     \sa QFontMetricsF::xHeight() */
end_comment
begin_function
DECL|function|xHeight
name|qreal
name|QRawFont
operator|::
name|xHeight
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|xHeight
argument_list|()
operator|.
name|toReal
argument_list|()
else|:
literal|0.0
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the leading of this QRawFont in pixel units.     \sa QFontMetricsF::leading() */
end_comment
begin_function
DECL|function|leading
name|qreal
name|QRawFont
operator|::
name|leading
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|leading
argument_list|()
operator|.
name|toReal
argument_list|()
else|:
literal|0.0
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the average character width of this QRawFont in pixel units.     \sa QFontMetricsF::averageCharWidth() */
end_comment
begin_function
DECL|function|averageCharWidth
name|qreal
name|QRawFont
operator|::
name|averageCharWidth
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|averageCharWidth
argument_list|()
operator|.
name|toReal
argument_list|()
else|:
literal|0.0
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the width of the widest character in the font.     \sa QFontMetricsF::maxWidth() */
end_comment
begin_function
DECL|function|maxCharWidth
name|qreal
name|QRawFont
operator|::
name|maxCharWidth
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|maxCharWidth
argument_list|()
else|:
literal|0.0
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the pixel size set for this QRawFont. The pixel size affects how glyphs are    rasterized, the size of glyphs returned by pathForGlyph(), and is used to convert    internal metrics from design units to logical pixel units.     \sa setPixelSize() */
end_comment
begin_function
DECL|function|pixelSize
name|qreal
name|QRawFont
operator|::
name|pixelSize
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|fontDef
operator|.
name|pixelSize
else|:
literal|0.0
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the number of design units define the width and height of the em square    for this QRawFont. This value is used together with the pixel size when converting design metrics    to pixel units, as the internal metrics are specified in design units and the pixel size gives    the size of 1 em in pixels.     \sa pixelSize(), setPixelSize() */
end_comment
begin_function
DECL|function|unitsPerEm
name|qreal
name|QRawFont
operator|::
name|unitsPerEm
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|emSquareSize
argument_list|()
operator|.
name|toReal
argument_list|()
else|:
literal|0.0
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the thickness for drawing lines (underline, overline, etc.)    along with text drawn in this font.  */
end_comment
begin_function
DECL|function|lineThickness
name|qreal
name|QRawFont
operator|::
name|lineThickness
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|lineThickness
argument_list|()
operator|.
name|toReal
argument_list|()
else|:
literal|0.0
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the position from baseline for drawing underlines below the text    rendered with this font.  */
end_comment
begin_function
DECL|function|underlinePosition
name|qreal
name|QRawFont
operator|::
name|underlinePosition
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|underlinePosition
argument_list|()
operator|.
name|toReal
argument_list|()
else|:
literal|0.0
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the family name of this QRawFont. */
end_comment
begin_function
DECL|function|familyName
name|QString
name|QRawFont
operator|::
name|familyName
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|fontDef
operator|.
name|family
else|:
name|QString
argument_list|()
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the style name of this QRawFont.     \sa QFont::styleName() */
end_comment
begin_function
DECL|function|styleName
name|QString
name|QRawFont
operator|::
name|styleName
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|fontEngine
operator|->
name|fontDef
operator|.
name|styleName
else|:
name|QString
argument_list|()
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the style of this QRawFont.     \sa QFont::style() */
end_comment
begin_function
DECL|function|style
name|QFont
operator|::
name|Style
name|QRawFont
operator|::
name|style
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|QFont
operator|::
name|Style
argument_list|(
name|d
operator|->
name|fontEngine
operator|->
name|fontDef
operator|.
name|style
argument_list|)
else|:
name|QFont
operator|::
name|StyleNormal
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the weight of this QRawFont.     \sa QFont::weight() */
end_comment
begin_function
DECL|function|weight
name|int
name|QRawFont
operator|::
name|weight
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|int
argument_list|(
name|d
operator|->
name|fontEngine
operator|->
name|fontDef
operator|.
name|weight
argument_list|)
else|:
operator|-
literal|1
return|;
block|}
end_function
begin_comment
comment|/*!    Converts the string of unicode points given by \a text to glyph indexes    using the CMAP table in the underlying font, and returns a vector containing    the result.     Note that, in cases where there are other tables in the font that affect the    shaping of the text, the returned glyph indexes will not correctly represent    the rendering of the text. To get the correctly shaped text, you can use    QTextLayout to lay out and shape the text, then call QTextLayout::glyphs()    to get the set of glyph index list and QRawFont pairs.     \sa advancesForGlyphIndexes(), glyphIndexesForChars(), QGlyphRun, QTextLayout::glyphRuns(), QTextFragment::glyphRuns() */
end_comment
begin_function
DECL|function|glyphIndexesForString
name|QVector
argument_list|<
name|quint32
argument_list|>
name|QRawFont
operator|::
name|glyphIndexesForString
parameter_list|(
specifier|const
name|QString
modifier|&
name|text
parameter_list|)
specifier|const
block|{
name|QVector
argument_list|<
name|quint32
argument_list|>
name|glyphIndexes
decl_stmt|;
if|if
condition|(
operator|!
name|d
operator|->
name|isValid
argument_list|()
operator|||
name|text
operator|.
name|isEmpty
argument_list|()
condition|)
return|return
name|glyphIndexes
return|;
name|int
name|numGlyphs
init|=
name|text
operator|.
name|size
argument_list|()
decl_stmt|;
name|glyphIndexes
operator|.
name|resize
argument_list|(
name|numGlyphs
argument_list|)
expr_stmt|;
name|QGlyphLayout
name|glyphs
decl_stmt|;
name|glyphs
operator|.
name|numGlyphs
operator|=
name|numGlyphs
expr_stmt|;
name|glyphs
operator|.
name|glyphs
operator|=
name|glyphIndexes
operator|.
name|data
argument_list|()
expr_stmt|;
if|if
condition|(
operator|!
name|d
operator|->
name|fontEngine
operator|->
name|stringToCMap
argument_list|(
name|text
operator|.
name|data
argument_list|()
argument_list|,
name|text
operator|.
name|size
argument_list|()
argument_list|,
operator|&
name|glyphs
argument_list|,
operator|&
name|numGlyphs
argument_list|,
name|QFontEngine
operator|::
name|GlyphIndicesOnly
argument_list|)
condition|)
block|{
name|glyphIndexes
operator|.
name|resize
argument_list|(
name|numGlyphs
argument_list|)
expr_stmt|;
name|glyphs
operator|.
name|numGlyphs
operator|=
name|numGlyphs
expr_stmt|;
name|glyphs
operator|.
name|glyphs
operator|=
name|glyphIndexes
operator|.
name|data
argument_list|()
expr_stmt|;
if|if
condition|(
operator|!
name|d
operator|->
name|fontEngine
operator|->
name|stringToCMap
argument_list|(
name|text
operator|.
name|data
argument_list|()
argument_list|,
name|text
operator|.
name|size
argument_list|()
argument_list|,
operator|&
name|glyphs
argument_list|,
operator|&
name|numGlyphs
argument_list|,
name|QFontEngine
operator|::
name|GlyphIndicesOnly
argument_list|)
condition|)
block|{
name|Q_ASSERT_X
argument_list|(
literal|false
argument_list|,
name|Q_FUNC_INFO
argument_list|,
literal|"stringToCMap shouldn't fail twice"
argument_list|)
expr_stmt|;
return|return
name|QVector
argument_list|<
name|quint32
argument_list|>
argument_list|()
return|;
block|}
block|}
name|glyphIndexes
operator|.
name|resize
argument_list|(
name|numGlyphs
argument_list|)
expr_stmt|;
return|return
name|glyphIndexes
return|;
block|}
end_function
begin_comment
comment|/*!    Converts a string of unicode points to glyph indexes using the CMAP table in the    underlying font. The function works like glyphIndexesForString() except it take    an array (\a chars), the results will be returned though \a glyphIndexes array    and number of glyphs will be set in \a numGlyphs. The size of \a glyphIndexes array    must be at least \a numChars, if that's still not enough, this function will return    false, then you can resize \a glyphIndexes from the size returned in \a numGlyphs.     \sa glyphIndexesForString(), advancesForGlyphIndexes(), QGlyphRun, QTextLayout::glyphRuns(), QTextFragment::glyphRuns() */
end_comment
begin_function
DECL|function|glyphIndexesForChars
name|bool
name|QRawFont
operator|::
name|glyphIndexesForChars
parameter_list|(
specifier|const
name|QChar
modifier|*
name|chars
parameter_list|,
name|int
name|numChars
parameter_list|,
name|quint32
modifier|*
name|glyphIndexes
parameter_list|,
name|int
modifier|*
name|numGlyphs
parameter_list|)
specifier|const
block|{
name|Q_ASSERT
argument_list|(
name|numGlyphs
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|d
operator|->
name|isValid
argument_list|()
operator|||
name|numChars
operator|<=
literal|0
condition|)
block|{
operator|*
name|numGlyphs
operator|=
literal|0
expr_stmt|;
return|return
literal|false
return|;
block|}
if|if
condition|(
operator|*
name|numGlyphs
operator|<=
literal|0
operator|||
operator|!
name|glyphIndexes
condition|)
block|{
operator|*
name|numGlyphs
operator|=
name|numChars
expr_stmt|;
return|return
literal|false
return|;
block|}
name|QGlyphLayout
name|glyphs
decl_stmt|;
name|glyphs
operator|.
name|numGlyphs
operator|=
operator|*
name|numGlyphs
expr_stmt|;
name|glyphs
operator|.
name|glyphs
operator|=
name|glyphIndexes
expr_stmt|;
return|return
name|d
operator|->
name|fontEngine
operator|->
name|stringToCMap
argument_list|(
name|chars
argument_list|,
name|numChars
argument_list|,
operator|&
name|glyphs
argument_list|,
name|numGlyphs
argument_list|,
name|QFontEngine
operator|::
name|GlyphIndicesOnly
argument_list|)
return|;
block|}
end_function
begin_comment
comment|/*!     \fn QVector<QPointF> QRawFont::advancesForGlyphIndexes(const QVector<quint32>&glyphIndexes) const     Returns the QRawFont's advances for each of the \a glyphIndexes in pixel units. The advances    give the distance from the position of a given glyph to where the next glyph should be drawn    to make it appear as if the two glyphs are unspaced.     \sa QTextLine::horizontalAdvance(), QFontMetricsF::width() */
end_comment
begin_comment
comment|/*!    Returns the QRawFont's advances for each of the \a glyphIndexes in pixel units. The advances    give the distance from the position of a given glyph to where the next glyph should be drawn    to make it appear as if the two glyphs are unspaced. The glyph indexes are given with the    array \a glyphIndexes while the results are returned through \a advances, both of them must    have \a numGlyphs elements.     \sa QTextLine::horizontalAdvance(), QFontMetricsF::width() */
end_comment
begin_function
DECL|function|advancesForGlyphIndexes
name|bool
name|QRawFont
operator|::
name|advancesForGlyphIndexes
parameter_list|(
specifier|const
name|quint32
modifier|*
name|glyphIndexes
parameter_list|,
name|QPointF
modifier|*
name|advances
parameter_list|,
name|int
name|numGlyphs
parameter_list|)
specifier|const
block|{
name|Q_ASSERT
argument_list|(
name|glyphIndexes
operator|&&
name|advances
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|d
operator|->
name|isValid
argument_list|()
operator|||
name|numGlyphs
operator|<=
literal|0
condition|)
return|return
literal|false
return|;
name|QGlyphLayout
name|glyphs
decl_stmt|;
name|glyphs
operator|.
name|glyphs
operator|=
cast|const_cast
argument_list|<
name|HB_Glyph
operator|*
argument_list|>
argument_list|(
name|glyphIndexes
argument_list|)
expr_stmt|;
name|glyphs
operator|.
name|numGlyphs
operator|=
name|numGlyphs
expr_stmt|;
name|QVarLengthArray
argument_list|<
name|QFixed
argument_list|>
name|advances_x
argument_list|(
name|numGlyphs
argument_list|)
decl_stmt|;
name|QVarLengthArray
argument_list|<
name|QFixed
argument_list|>
name|advances_y
argument_list|(
name|numGlyphs
argument_list|)
decl_stmt|;
name|glyphs
operator|.
name|advances_x
operator|=
name|advances_x
operator|.
name|data
argument_list|()
expr_stmt|;
name|glyphs
operator|.
name|advances_y
operator|=
name|advances_y
operator|.
name|data
argument_list|()
expr_stmt|;
name|d
operator|->
name|fontEngine
operator|->
name|recalcAdvances
argument_list|(
operator|&
name|glyphs
argument_list|,
literal|0
argument_list|)
expr_stmt|;
for|for
control|(
name|int
name|i
init|=
literal|0
init|;
name|i
operator|<
name|numGlyphs
condition|;
operator|++
name|i
control|)
name|advances
index|[
name|i
index|]
operator|=
name|QPointF
argument_list|(
name|glyphs
operator|.
name|advances_x
index|[
name|i
index|]
operator|.
name|toReal
argument_list|()
argument_list|,
name|glyphs
operator|.
name|advances_y
index|[
name|i
index|]
operator|.
name|toReal
argument_list|()
argument_list|)
expr_stmt|;
return|return
literal|true
return|;
block|}
end_function
begin_comment
comment|/*!    Returns the hinting preference used to construct this QRawFont.     \sa QFont::hintingPreference() */
end_comment
begin_function
DECL|function|hintingPreference
name|QFont
operator|::
name|HintingPreference
name|QRawFont
operator|::
name|hintingPreference
parameter_list|()
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
condition|?
name|d
operator|->
name|hintingPreference
else|:
name|QFont
operator|::
name|PreferDefaultHinting
return|;
block|}
end_function
begin_comment
comment|/*!    Retrieves the sfnt table named \a tagName from the underlying physical font, or an empty    byte array if no such table was found. The returned font table's byte order is Big Endian, like    the sfnt format specifies. The \a tagName must be four characters long and should be formatted    in the default endianness of the current platform. */
end_comment
begin_function
DECL|function|fontTable
name|QByteArray
name|QRawFont
operator|::
name|fontTable
parameter_list|(
specifier|const
name|char
modifier|*
name|tagName
parameter_list|)
specifier|const
block|{
if|if
condition|(
operator|!
name|d
operator|->
name|isValid
argument_list|()
condition|)
return|return
name|QByteArray
argument_list|()
return|;
specifier|const
name|quint32
modifier|*
name|tagId
init|=
cast|reinterpret_cast
argument_list|<
specifier|const
name|quint32
operator|*
argument_list|>
argument_list|(
name|tagName
argument_list|)
decl_stmt|;
return|return
name|d
operator|->
name|fontEngine
operator|->
name|getSfntTable
argument_list|(
name|qToBigEndian
argument_list|(
operator|*
name|tagId
argument_list|)
argument_list|)
return|;
block|}
end_function
begin_comment
comment|// From qfontdatabase.cpp
end_comment
begin_function_decl
specifier|extern
name|QList
argument_list|<
name|QFontDatabase
operator|::
name|WritingSystem
argument_list|>
name|qt_determine_writing_systems_from_truetype_bits
parameter_list|(
name|quint32
name|unicodeRange
index|[
literal|4
index|]
parameter_list|,
name|quint32
name|codePageRange
index|[
literal|2
index|]
parameter_list|)
function_decl|;
end_function_decl
begin_comment
comment|/*!    Returns a list of writing systems supported by the font according to designer supplied    information in the font file. Please note that this does not guarantee support for a    specific unicode point in the font. You can use the supportsCharacter() to check support    for a single, specific character.     \note The list is determined based on the unicode ranges and codepage ranges set in the font's    OS/2 table and requires such a table to be present in the underlying font file.     \sa supportsCharacter() */
end_comment
begin_function
DECL|function|supportedWritingSystems
name|QList
argument_list|<
name|QFontDatabase
operator|::
name|WritingSystem
argument_list|>
name|QRawFont
operator|::
name|supportedWritingSystems
parameter_list|()
specifier|const
block|{
if|if
condition|(
name|d
operator|->
name|isValid
argument_list|()
condition|)
block|{
name|QByteArray
name|os2Table
init|=
name|fontTable
argument_list|(
literal|"OS/2"
argument_list|)
decl_stmt|;
if|if
condition|(
name|os2Table
operator|.
name|size
argument_list|()
operator|>
literal|86
condition|)
block|{
name|char
modifier|*
name|data
init|=
name|os2Table
operator|.
name|data
argument_list|()
decl_stmt|;
name|quint32
modifier|*
name|bigEndianUnicodeRanges
init|=
cast|reinterpret_cast
argument_list|<
name|quint32
operator|*
argument_list|>
argument_list|(
name|data
operator|+
literal|42
argument_list|)
decl_stmt|;
name|quint32
modifier|*
name|bigEndianCodepageRanges
init|=
cast|reinterpret_cast
argument_list|<
name|quint32
operator|*
argument_list|>
argument_list|(
name|data
operator|+
literal|78
argument_list|)
decl_stmt|;
name|quint32
name|unicodeRanges
index|[
literal|4
index|]
decl_stmt|;
name|quint32
name|codepageRanges
index|[
literal|2
index|]
decl_stmt|;
for|for
control|(
name|int
name|i
init|=
literal|0
init|;
name|i
operator|<
literal|4
condition|;
operator|++
name|i
control|)
block|{
if|if
condition|(
name|i
operator|<
literal|2
condition|)
name|codepageRanges
index|[
name|i
index|]
operator|=
name|qFromBigEndian
argument_list|(
name|bigEndianCodepageRanges
index|[
name|i
index|]
argument_list|)
expr_stmt|;
name|unicodeRanges
index|[
name|i
index|]
operator|=
name|qFromBigEndian
argument_list|(
name|bigEndianUnicodeRanges
index|[
name|i
index|]
argument_list|)
expr_stmt|;
block|}
return|return
name|qt_determine_writing_systems_from_truetype_bits
argument_list|(
name|unicodeRanges
argument_list|,
name|codepageRanges
argument_list|)
return|;
block|}
block|}
return|return
name|QList
argument_list|<
name|QFontDatabase
operator|::
name|WritingSystem
argument_list|>
argument_list|()
return|;
block|}
end_function
begin_comment
comment|/*!     Returns true if the font has a glyph that corresponds to the given \a character.      \sa supportedWritingSystems() */
end_comment
begin_function
DECL|function|supportsCharacter
name|bool
name|QRawFont
operator|::
name|supportsCharacter
parameter_list|(
name|QChar
name|character
parameter_list|)
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
operator|&&
name|d
operator|->
name|fontEngine
operator|->
name|canRender
argument_list|(
operator|&
name|character
argument_list|,
literal|1
argument_list|)
return|;
block|}
end_function
begin_comment
comment|/*!     \overload     Returns true if the font has a glyph that corresponds to the UCS-4 encoded character \a ucs4.     \sa supportedWritingSystems() */
end_comment
begin_function
DECL|function|supportsCharacter
name|bool
name|QRawFont
operator|::
name|supportsCharacter
parameter_list|(
name|uint
name|ucs4
parameter_list|)
specifier|const
block|{
return|return
name|d
operator|->
name|isValid
argument_list|()
operator|&&
name|d
operator|->
name|fontEngine
operator|->
name|canRender
argument_list|(
name|ucs4
argument_list|)
return|;
block|}
end_function
begin_comment
comment|// qfontdatabase.cpp
end_comment
begin_function_decl
specifier|extern
name|int
name|qt_script_for_writing_system
parameter_list|(
name|QFontDatabase
operator|::
name|WritingSystem
name|writingSystem
parameter_list|)
function_decl|;
end_function_decl
begin_comment
comment|/*!    Fetches the physical representation based on a \a font query. The physical font returned is    the font that will be preferred by Qt in order to display text in the selected \a writingSystem.     \warning This function is potentially expensive and should not be called in performance    sensitive code. */
end_comment
begin_function
DECL|function|fromFont
name|QRawFont
name|QRawFont
operator|::
name|fromFont
parameter_list|(
specifier|const
name|QFont
modifier|&
name|font
parameter_list|,
name|QFontDatabase
operator|::
name|WritingSystem
name|writingSystem
parameter_list|)
block|{
name|QRawFont
name|rawFont
decl_stmt|;
name|QFontPrivate
modifier|*
name|font_d
init|=
name|QFontPrivate
operator|::
name|get
argument_list|(
name|font
argument_list|)
decl_stmt|;
name|int
name|script
init|=
name|qt_script_for_writing_system
argument_list|(
name|writingSystem
argument_list|)
decl_stmt|;
name|QFontEngine
modifier|*
name|fe
init|=
name|font_d
operator|->
name|engineForScript
argument_list|(
name|script
argument_list|)
decl_stmt|;
if|if
condition|(
name|fe
operator|!=
literal|0
operator|&&
name|fe
operator|->
name|type
argument_list|()
operator|==
name|QFontEngine
operator|::
name|Multi
condition|)
block|{
name|QFontEngineMulti
modifier|*
name|multiEngine
init|=
cast|static_cast
argument_list|<
name|QFontEngineMulti
operator|*
argument_list|>
argument_list|(
name|fe
argument_list|)
decl_stmt|;
name|fe
operator|=
name|multiEngine
operator|->
name|engine
argument_list|(
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|fe
operator|==
literal|0
condition|)
block|{
name|multiEngine
operator|->
name|loadEngine
argument_list|(
literal|0
argument_list|)
expr_stmt|;
name|fe
operator|=
name|multiEngine
operator|->
name|engine
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
block|}
if|if
condition|(
name|fe
operator|!=
literal|0
condition|)
block|{
name|rawFont
operator|.
name|d
operator|.
name|data
argument_list|()
operator|->
name|fontEngine
operator|=
name|fe
expr_stmt|;
name|rawFont
operator|.
name|d
operator|.
name|data
argument_list|()
operator|->
name|fontEngine
operator|->
name|ref
operator|.
name|ref
argument_list|()
expr_stmt|;
name|rawFont
operator|.
name|d
operator|.
name|data
argument_list|()
operator|->
name|hintingPreference
operator|=
name|font
operator|.
name|hintingPreference
argument_list|()
expr_stmt|;
block|}
return|return
name|rawFont
return|;
block|}
end_function
begin_comment
comment|/*!    Sets the pixel size with which this font should be rendered to \a pixelSize. */
end_comment
begin_function
DECL|function|setPixelSize
name|void
name|QRawFont
operator|::
name|setPixelSize
parameter_list|(
name|qreal
name|pixelSize
parameter_list|)
block|{
if|if
condition|(
name|d
operator|->
name|fontEngine
operator|==
literal|0
operator|||
name|qFuzzyCompare
argument_list|(
name|d
operator|->
name|fontEngine
operator|->
name|fontDef
operator|.
name|pixelSize
argument_list|,
name|pixelSize
argument_list|)
condition|)
return|return;
name|d
operator|.
name|detach
argument_list|()
expr_stmt|;
name|QFontEngine
modifier|*
name|oldFontEngine
init|=
name|d
operator|->
name|fontEngine
decl_stmt|;
name|d
operator|->
name|fontEngine
operator|=
name|d
operator|->
name|fontEngine
operator|->
name|cloneWithSize
argument_list|(
name|pixelSize
argument_list|)
expr_stmt|;
if|if
condition|(
name|d
operator|->
name|fontEngine
operator|!=
literal|0
condition|)
name|d
operator|->
name|fontEngine
operator|->
name|ref
operator|.
name|ref
parameter_list|()
constructor_decl|;
name|oldFontEngine
operator|->
name|ref
operator|.
name|deref
argument_list|()
expr_stmt|;
if|if
condition|(
name|oldFontEngine
operator|->
name|cache_count
operator|==
literal|0
operator|&&
name|oldFontEngine
operator|->
name|ref
operator|.
name|load
argument_list|()
operator|==
literal|0
condition|)
operator|delete
name|oldFontEngine
expr_stmt|;
block|}
end_function
begin_comment
comment|/*!     \internal */
end_comment
begin_function
DECL|function|cleanUp
name|void
name|QRawFontPrivate
operator|::
name|cleanUp
parameter_list|()
block|{
name|platformCleanUp
argument_list|()
expr_stmt|;
if|if
condition|(
name|fontEngine
operator|!=
literal|0
condition|)
block|{
name|fontEngine
operator|->
name|ref
operator|.
name|deref
argument_list|()
expr_stmt|;
if|if
condition|(
name|fontEngine
operator|->
name|cache_count
operator|==
literal|0
operator|&&
name|fontEngine
operator|->
name|ref
operator|.
name|load
argument_list|()
operator|==
literal|0
condition|)
operator|delete
name|fontEngine
expr_stmt|;
name|fontEngine
operator|=
literal|0
expr_stmt|;
block|}
name|hintingPreference
operator|=
name|QFont
operator|::
name|PreferDefaultHinting
expr_stmt|;
block|}
end_function
begin_comment
comment|/*!   Returns the smallest rectangle containing the glyph with the given \a glyphIndex.    \since 5.0 */
end_comment
begin_function
DECL|function|boundingRect
name|QRectF
name|QRawFont
operator|::
name|boundingRect
parameter_list|(
name|quint32
name|glyphIndex
parameter_list|)
specifier|const
block|{
if|if
condition|(
operator|!
name|isValid
argument_list|()
condition|)
return|return
name|QRectF
argument_list|()
return|;
name|glyph_metrics_t
name|gm
init|=
name|d
operator|->
name|fontEngine
operator|->
name|boundingBox
argument_list|(
name|glyphIndex
argument_list|)
decl_stmt|;
return|return
name|QRectF
argument_list|(
name|gm
operator|.
name|x
operator|.
name|toReal
argument_list|()
argument_list|,
name|gm
operator|.
name|y
operator|.
name|toReal
argument_list|()
argument_list|,
name|gm
operator|.
name|width
operator|.
name|toReal
argument_list|()
argument_list|,
name|gm
operator|.
name|height
operator|.
name|toReal
argument_list|()
argument_list|)
return|;
block|}
end_function
begin_endif
endif|#
directive|endif
end_endif
begin_comment
comment|// QT_NO_RAWFONT
end_comment
begin_macro
name|QT_END_NAMESPACE
end_macro
end_unit
